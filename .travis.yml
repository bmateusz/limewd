languate: c
dist: trusty
sudo: false
addons:
  apt:
    packages:
    - libmicrohttpd-dev
    - libsqlite3-dev
    - duktape-dev
    - libgcrypt20-dev
    - libgnutls-dev
    - doxygen
    - graphviz
