languate: c
os: linux
sudo: required
branches:
  only:
    - master
addons:
  apt:
    packages:
    - libgcrypt-dev
    - libgnutls-dev
    - doxygen
    - graphviz
install:
  - ./init.sh libs
  - sudo ./init.sh install
script:
  - make
  - ./test.sh
  - ./lmwd -v
after_success:
  - ./gendoc.sh
