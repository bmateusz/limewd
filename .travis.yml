languate: c
os: linux
sudo: false
branches:
  only:
    - master
addons:
  apt:
    packages:
    - libgcrypt-dev
    - libgnutls-dev
    - doxygen
    - graphviz
install:
  - ./init.sh local libs install
  - export LD_LIBRARY_PATH="$(pwd)/compiled/lib"
script:
  - make
  - ./test.sh
  - ./lmwd -v
after_success:
  - ./gendoc.sh
