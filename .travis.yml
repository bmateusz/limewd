languate: c
os: linux
sudo: required
addons:
  apt:
    packages:
    - libgcrypt-dev
    - libgnutls-dev
    - doxygen
    - graphviz
install:
  - ./init.sh libs
  - sudo ./init.sh install
script:
  - make
  - make doc
  - ./test.sh
  - ./lmwd -v
